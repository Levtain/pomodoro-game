# 三权分立 - 技术实现方案

## 🤖 核心理念

通过报务员脚本、JSON文件、Hook机制，实现三长老院之间的自动协调和通知。

---

## 🏛️ 角色定义

### 上议院（主会证 - 我）
- **角色：** 协调、决策、最终汇总
- **任务：** 
  - 头脑风暴（使用Brainstorming skill）
  - 整合各方意见
  - 最终决策
  - 通知执行层

### 长老院（3个agent）
- **角色：** 专业质疑、找茬、提供建议
- **任务：**
  - 收到提案后立即质疑
  - 找专家思维模式
  - 反驳提案
  - 挑毛病
  - 提出改进方案
  - 互相通知（避免重复）

### 报务员脚本
- **角色：** 协调员、通知中心
- **任务：**
  - 读取JSON文件
  - 通知下一个长老
  - 传递提案和信息
  - 汇总状态给上议院

### 干活Agent（每个长老一个）
- **角色：** 搜索、总结、文档化
- **任务：**
  - 根据长老的指示搜索资料
  - 总结成文档
  - 提交到指定位置

---

## 🔄 三阶段工作流程

### 阶段1：头脑风暴（上议院 + 主人）

```
1. 我（上议院）使用Brainstorming skill
   - 逐步问问题
   - 形成初步提案
   
2. 讨论完成
   ↓
3. 我生成初步提案文档
   ↓
4. 报务员脚本启动阶段2
```

**报务员操作：**
```bash
# 读取当前状态
cat /tmp/senate-status.json

# 应该是
{
  "stage": "brainstorming",
  "status": "completed",
  "proposal": "docs/提案-初步.md"
}

# 启动阶段2：长老院辩论
# 通知三个长老
```

---

### 阶段2：长老院辩论（3个长老 + 报务员）

```
1. 报务员读取提案
   ↓
2. 通知长老A（逻辑批判）
   → 长老A修改JSON，选定方向
   ↓
3. 报务员通知长老B（技术可行性）
   → 长老B修改JSON，选定方向
   ↓
4. 报务员通知长老C（实用性）
   → 长老C修改JSON，选定方向
   ↓
5. 三个长老同时开始质疑
   ↓
6. 互相通知（避免重复）
   ↓
7. 干活agent整理意见
   ↓
8. 长老总结，生成修改案
   ↓
9. 报务员通知上议院
```

**JSON文件结构（/tmp/senate-status.json）：**
```json
{
  "stage": "debate",
  "status": "in_progress",
  "proposal": "docs/提案-初步.md",
  "elders": {
    "elder-a": {
      "status": "working",
      "direction": "logic-critique",
      "assignedAt": "2026-02-09T11:57:00Z",
      "agent": "worker-a"
    },
    "elder-b": {
      "status": "waiting",
      "direction": null,
      "agent": "worker-b"
    },
    "elder-c": {
      "status": "waiting",
      "direction": null,
      "agent": "worker-c"
    }
  }
}
```

**报务员脚本（notify_elder.py）：**
```python
import json
import os
from datetime import datetime

def read_status():
    with open('/tmp/senate-status.json', 'r') as f:
        return json.load(f)

def write_status(status):
    with open('/tmp/senate-status.json', 'w') as f:
        json.dump(status, f, indent=2)

def notify_elder(elder_id, proposal_path, direction):
    """通知长老开始质疑"""
    print(f"[报务员] 通知长老的{elder_id}开始质疑")
    print(f"[报务员] 提案：{proposal_path}")
    print(f"[报务员] 选定方向：{direction}")
    print(f"[报务员] 时间：{datetime.now().isoformat()}")
    
    # 这里可以调用sessions_spawn
    # sessions_spawn({
    #   task: f"作为长老的{elder_id}，收到提案：{proposal_path}",
    #   label: f"elder-{elder_id}",
    #   ...
    # })

def notify_next_elder(current_status):
    """通知下一个长老"""
    status = read_status()
    
    # 找到下一个等待的长老
    next_elder = None
    for elder_id, elder_info in status['elders'].items():
        if elder_info['status'] == 'waiting':
            next_elder = elder_id
            break
    
    if next_elder:
        print(f"[报务员] 通知长老的{next_elder}可以开始了")
        # 通知下一个长老
        # 这里可以读取他们的选定的方向
        
    else:
        print(f"[报务员] 所有长老已完成")
        # 进入阶段3

def update_elder_direction(elder_id, direction):
    """长老选定方向"""
    status = read_status()
    status['elders'][elder_id]['direction'] = direction
    status['elders'][elder_id]['status'] = 'working'
    status['elders'][elder_id]['assignedAt'] = datetime.now().isoformat()
    write_status(status)
    print(f"[报务员] 长老的{elder_id}选定方向：{direction}")

def elder_complete(elder_id, report_path):
    """长老完成质疑"""
    status = read_status()
    status['elders'][elder_id]['status'] = 'completed'
    status['elders'][elder_id]['report'] = report_path
    write_status(status)
    print(f"[报务员] 长老的{elder_id}完成质疑")
    print(f"[报务员] 报告：{report_path}")

def main():
    # 阶段2开始
    print("=== 阶段2：长老院辩论 ===")
    
    # 读取当前状态
    status = read_status()
    
    # 检查是否在正确的阶段
    if status['stage'] != 'debate':
        print(f"错误：当前阶段是{status['stage']}，不是debate")
        return
    
    # 按顺序通知长老
    elders_order = ['elder-a', 'elder-b', 'elder-c']
    
    for elder_id in elders_order:
        # 通知长老
        direction = status['elders'][elder_id].get('direction')
        if direction:
            print(f"[报务员] 长老的{elder_id}已选定方向，可以开始")
        else:
            print(f"[报务员] 等待长老的{elder_id}选定方向")
            # 这里需要等待用户通过某个方式通知
            # 比如在Discord说："@长老A 你的方向是逻辑批判"
            # 或者通过JSON文件更新来触发
    
    # 所有长老完成后
    all_completed = all(
        elder_info['status'] == 'completed'
        for elder_info in status['elders'].values()
    )
    
    if all_completed:
        print("[报务员] 所有长老已完成，准备进入阶段3")
        # 通知上议院
        status['stage'] = 'brainstorming-revised'
        write_status(status)

if __name__ == '__main__':
    main()
```

---

### 阶段3：修改案Brainstorming（上议院 + 主人）

```
1. 报务员通知上议院
   ↓
2. 我读取修改案
   ↓
3. 我使用Brainstorming skill讨论
   - 如何改进提案
   - 每次一个问题
   - 形成最终方案
   ↓
4. 决策和任务分配
```

**报务员操作：**
```bash
# 读取当前状态
cat /tmp/senate-status.json

# 应该是
{
  "stage": "brainstorming-revised",
  "status": "ready",
  "proposal": "docs/提案-初步.md",
  "modification": "docs/修改案.md"
}

# 通知上议院
```

---

## 🔗 Hook机制

### Hook触发点

### 1. 长老选定方向
```javascript
// Hook: elder_direction_selected
// 触发：长老修改JSON文件

{
  "type": "elder",
  "action": "direction_selected",
  "elderId": "elder-a",
  "direction": "logic-critique",
  "timestamp": "2026-02-09T11:57:00Z"
}
```

### 2. 长老完成质疑
```javascript
// Hook: elder_completed
// 触发：长老完成报告

{
  "type": "elder",
  "action": "completed",
  "elderId": "elder-a",
  "report": "docs/长老A报告.md",
  "timestamp": "2026-02-09T12:15:00Z"
}
```

### 3. 所有长老完成
```javascript
// Hook: all_elders_completed
// 触发：最后一个长老完成

{
  "type": "senate",
  "action": "all_elders_completed",
  "stage": "debate",
  "status": "ready_for_next",
  "timestamp": "2026-02-09T12:45:00Z"
}
```

### Hook处理器实现
```javascript
// 在Clawdbot中实现Hook处理

// 注册Hook
registerInternalHook('elder:direction_selected', async (event) => {
  const elderId = event.elderId;
  const direction = event.direction;
  
  console.log(`[Hook] 长老的{elderId}选定方向：{direction}`);
  
  // 通知下一个长老（通过报务员脚本）
  // 这里可以调用exec执行脚本
  
  // 或者直接读取JSON文件更新状态
});

registerInternalHook('elder:completed', async (event) => {
  const elderId = event.elderId;
  const report = event.report;
  
  console.log(`[Hook] 长老的{elderId}完成质疑`);
  
  // 更新JSON状态
  // 检查是否所有长老都完成
  
  // 如果是最后一个，触发"所有长老完成"Hook
});

registerInternalHook('senate:all_elders_completed', async (event) => {
  console.log('[Hook] 所有长老完成');
  
  // 通知上议院
  // 进入阶段3
});
```

---

## 💬 通知机制（JSON + Hook）

### JSON文件状态管理

**文件位置：** `/tmp/senate-status.json`

**状态流转：**
```json
// 阶段1：头脑风暴
{
  "stage": "brainstorming",
  "status": "in_progress",
  "proposal": null
}

// 阶段1完成
{
  "stage": "brainstorming",
  "status": "completed",
  "proposal": "docs/提案-初步.md"
}

// 阶段2：辩论
{
  "stage": "debate",
  "status": "in_progress",
  "proposal": "docs/提案-初步.md",
  "elders": {
    "elder-a": {
      "status": "working",
      "direction": "logic-critique",
      "report": null
    },
    "elder-b": {
      "status": "waiting",
      "direction": null,
      "report": null
    },
    "elder-c": {
      "status": "waiting",
      "direction": null,
      "report": null
    }
  }
}

// 阶段2完成
{
  "stage": "debate",
  "status": "completed",
  "proposal": "docs/提案-初步.md",
  "modification": "docs/修改案.md",
  "elders": {
    "elder-a": {
      "status": "completed",
      "direction": "logic-critique",
      "report": "docs/长老A报告.md"
    },
    "elder-b": {
      "status": "completed",
      "direction": "technical-feasibility",
      "report": "docs/长老B报告.md"
    },
    "elder-c": {
      "status": "completed",
      "direction": "practicality",
      "report": "docs/长老C报告.md"
    }
  }
}

// 阶段3：修改案Brainstorming
{
  "stage": "brainstorming-revised",
  "status": "in_progress",
  "proposal": "docs/提案-初步.md",
  "modification": "docs/修改案.md"
}

// 阶段3完成
{
  "stage": "brainstorming-revised",
  "status": "completed",
  "proposal": "docs/提案-初步.md",
  "modification": "docs/修改案.md",
  "final": "docs/最终决策.md"
}
```

---

## 🤔 主人如何主动通知？

### 1. 阶段进度查询

主人可以通过Discord命令查询当前阶段：
```
/status

回复：
当前阶段：debate（长老院辩论中）
状态：in_progress
- 长老A：working（逻辑批判）
- 长老B：waiting
- 长老C：waiting
```

### 2. 阶段强制推进

主人可以强制推进到下一阶段：
```
/advance

回复：
当前阶段：debate
→ 推进到：brainstorming-revised（修改案讨论）

（会跳过等待中的长老）
```

### 3. 阶段重置

主人可以重置当前阶段：
```
/reset-stage

回复：
已重置到：brainstorming
状态：in_progress
```

---

## 🚀 完整工作流程示例

```
1. 主人："我要做个番茄钟游戏"

   ↓
2. 我（上议院）："好，开始头脑风暴"
   [使用Brainstorming skill，逐步提问]

   ↓
3. 我生成初步提案：docs/提案-初步.md
   更新JSON：{stage: "brainstorming", status: "completed"}

   ↓
4. 主人："开始长老院辩论"

   ↓
5. 报务员脚本启动：
   - 读取JSON
   - 通知长老A
   - 通知长老B
   - 通知长老C

   ↓
6. 长老A选定方向："逻辑批判"
   更新JSON：elders.elder-a.direction = "logic-critique"
   触发Hook：elder:direction_selected

   ↓
7. 报务员通知长老B：
   读取JSON
   发现长老A已选定方向
   通知长老B可以开始

   ↓
8. 长老B选定方向："技术可行性"
   更新JSON：elders.elder-b.direction = "technical-feasibility"
   触发Hook：elder:direction_selected

   ↓
9. 报务员通知长老C：
   读取JSON
   发现长老A、B已选定方向
   通知长老C可以开始

   ↓
10. 长老C选定方向："实用性"
   更新JSON：elders.elder-c.direction = "practicality"
   触发Hook：elder:direction_selected

   ↓
11. 三个长老同时质疑
   - 干活agent搜索、整理
   - 互相通知避免重复

   ↓
12. 长老A完成：
   生成：docs/长老A报告.md
   更新JSON：elders.elder-a.status = "completed"
   触发Hook：elder:completed

   ↓
13. 长老B完成：
   生成：docs/长老B报告.md
   更新JSON：elders.elder-b.status = "completed"
   触发Hook：elder:completed

   ↓
14. 长老C完成：
   生成：docs/长老C报告.md
   更新JSON：elders.elder-c.status = "completed"
   触发Hook：elder:completed

   ↓
15. 检查所有长老完成
   触发Hook：senate:all_elders_completed

   ↓
16. 报务员通知上议院：
   "所有长老已完成，开始修改案Brainstorming"
   更新JSON：{stage: "brainstorming-revised", status: "in_progress"}

   ↓
17. 我（上议院）："收到，开始修改案Brainstorming"
   读取修改案：docs/修改案.md
   使用Brainstorming skill讨论改进

   ↓
18. 最终决策和任务分配
```

---

## 🎯 总结

**核心技术方案：**
- ✅ 报务员脚本（协调、通知）
- ✅ JSON文件（状态管理）
- ✅ Hook机制（事件触发）
- ✅ 干活Agent（执行）

**关键优势：**
- 自动协调
- 状态透明
- 灵活可控
- 可查询进度

---

**创建时间：** 2026-02-07
**作者：** 二狗 + 蜡烛
**状态：** 提案，待讨论
